# 📊 Инструкции: Подготовка данных для обучения

## ⚠️ КРИТИЧЕСКИ ВАЖНО

При обучении моделей formal/informal классификации без правильной подготовки данных возникает проблема **"идеальной точности"** - модели достигают 99-100% accuracy, но на самом деле обучаются на различиях в **длине текстов**, а не на содержании.

**Причина:** Формальные тексты (новости) обычно длиннее неформальных (соцсети).

**Решение:** Две обязательные техники:
1. **Length Normalization** - выравнивание распределения длин
2. **Noise Injection** - добавление шума для обобщения

---

## 📁 Структура файлов

```
data/
├── prepare_data.py        # НОВЫЙ: Скрипт подготовки
├── train.csv              # Готовый датасет
├── val.csv                # Валидационный набор (опционально)
└── test.csv               # Тестовый набор (опционально)
```

---

## 1️⃣ Создать `data/prepare_data.py`

Файл уже добавлен в проект: `data/prepare_data.py`.

---

## 2️⃣ Использование

### Подготовка данных

```bash
# Установка зависимостей
pip install datasets pandas numpy

# Подготовка данных с дефолтными параметрами
python data/prepare_data.py --output data/train.csv

# С настройками
python data/prepare_data.py \
    --output data/train.csv \
    --samples 5000 \
    --min-words 5 \
    --max-words 50 \
    --no-noise  # отключить аугментацию (не рекомендуется)
```

### Обучение моделей

```bash
# После подготовки данных
python ml/train_classic.py --data data/train.csv
python ml/train_bert.py --data data/train.csv --output models/bert
```

---

## ✅ Проверка распределения

- Mean длины formal ≈ mean длины informal (разница < 3 слов)
- Std похожие для обоих классов

---

## ⚠️ Признаки проблемы

| Признак | Что делать |
|---------|------------|
| F1 > 0.95 на тесте | Проверить распределение длин |
| Formal длиннее informal на >5 слов | Ужесточить length_range |
| Модель предсказывает по первым словам | Добавить больше шума |
| CV F1 >> Test F1 | Увеличить аугментацию |

---

## 📚 Ожидаемые результаты

| Модель | F1-macro |
|--------|----------|
| LogReg | 0.80-0.85 |
| SVM | 0.80-0.85 |
| NaiveBayes | 0.78-0.83 |
| RandomForest | 0.75-0.82 |
| BERT | 0.88-0.93 |
